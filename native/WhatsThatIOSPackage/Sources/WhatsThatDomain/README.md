# WhatsThatDomain

This module defines platform-agnostic business rules for WhatsThat. Keep types free of UI frameworks such as SwiftUI or UIKit and avoid depending on data or infrastructure implementations. Protocols that declare boundary contracts live here while concrete implementations belong to other packages.
